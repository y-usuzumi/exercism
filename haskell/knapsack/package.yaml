name: knapsack
version: 1.0.0.0

dependencies:
  - base

library:
  exposed-modules: Knapsack
  source-dirs: src
  ghc-options: -Wall
  dependencies:
  - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - knapsack
      - hspec
